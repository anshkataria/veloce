spring:
  application:
    name: veloce-api

  datasource:
    url: jdbc:postgresql://localhost:5432/veloce_db
    username: veloce_user
    password: veloce123
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

server:
  port: 8080

jwt:
  secret: veloce-super-secret-jwt-key-change-this-in-production-min-32-chars
  expiration: 86400000